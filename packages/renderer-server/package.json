{
  "name": "@euginejs/renderer-server",
  "version": "0.1.0",
  "description": "Deterministic, browser-API-free HTML renderer for Eugine documents. Safe to run in Node.js server contexts (e.g. Next.js Server Components) without loading the visual editor.",
  "type": "module",
  "license": "MIT",
  "sideEffects": false,
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@euginejs/core": "0.1.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "eugine",
    "ssr",
    "renderer",
    "server-rendering",
    "html"
  ],
  "engines": {
    "node": ">=18.18.0"
  }
}
