{
  "name": "@euginejs/renderer",
  "version": "0.1.0",
  "description": "Browser DOM renderer for Eugine documents, with localized (per-node) updates instead of full-document re-renders.",
  "type": "module",
  "license": "MIT",
  "sideEffects": false,
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@euginejs/core": "0.1.0"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "eugine",
    "renderer",
    "dom"
  ],
  "engines": {
    "node": ">=18.18.0"
  }
}
