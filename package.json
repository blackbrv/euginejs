{
  "name": "eugine-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Eugine – an extensible engine for building visual page builders and editors",
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @eugine/core -w @eugine/renderer -w @eugine/renderer-server -w eugine -w playground -w example-getting-started -w example-kitchen-sink --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsup": "^8.3.5",
    "vitest": "^2.1.4",
    "@vitest/coverage-v8": "^2.1.4"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "license": "MIT",
  "allowScripts": {
    "esbuild@0.27.7": true,
    "esbuild@0.21.5": true
  }
}
